<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain-HTTP (cleartext) traffic by default since API 28. NestHub.API runs over
  HTTP during local development (10.0.2.2 is the Android emulator's alias for the host machine's
  localhost), so this config carves out a narrow cleartext exception for local dev hosts only.
  Production builds should point MauiProgram.ApiBaseAddress at the deployed HTTPS API URL, which
  needs no exception here at all.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
